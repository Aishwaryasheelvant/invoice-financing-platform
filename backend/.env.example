DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=invoice_financing

# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_ACCESS_SECRET=hii this is env file
JWT_ACCESS_EXPIRES_IN_SECONDS=900
JWT_REFRESH_SECRET=this is refresh env text
JWT_REFRESH_EXPIRES_IN_SECONDS=604800

REDIS_HOST=localhost
REDIS_PORT=6379

# Days past the due date before an unpaid invoice defaults and the
# advance is recovered from the SME under recourse.
DEFAULT_GRACE_PERIOD_DAYS=30

# Cron pattern for the arrears scan: flags unpaid past-due invoices as
# overdue, then defaults them once the grace period above has elapsed.
# (BullMQ repeatable job.)
ARREARS_SCAN_CRON=0 0 * * *

# Global rate limit: THROTTLE_LIMIT requests per THROTTLE_TTL_MS per IP.
# Individual routes (e.g. auth/login) set a tighter limit of their own.
THROTTLE_TTL_MS=60000
THROTTLE_LIMIT=100

PORT=3000

# Comma-separated list of origins allowed to call this API from a browser.
# The Angular dev server runs on 4200 by default.
CORS_ORIGIN=http://localhost:4200
