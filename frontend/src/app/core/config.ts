/**
 * Deliberately not an Angular "environments/" setup (fileReplacements,
 * separate environment.prod.ts, etc.) — that's real complexity for a
 * detail this project doesn't need. One constant is enough.
 */
export const API_BASE_URL = 'http://localhost:3000';
